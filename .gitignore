# Don't track inspirational or default layouts
backups/
drashna/
default/
secrets.*
.DS_Store
.vscode/

# Don't track build backups
*.bin
*.hex

# Only commit the example so I can stop overwriting when I switch machines -.-
/tools/qmkws.cfg